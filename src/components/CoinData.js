import React from 'react'

const CoinData = (props) => {

    const renderData = () => {
        
    }

    return (
        <div>
            {renderData()}
        </div>
    )
}

export default CoinData
